#Faça um Programa que converta metros para centímetros.

metros = float(input('Informe os metros: '))
centimetros = metros*100
print('{} metros equivalem a {:.0f} centímetros.'.format(metros, centimetros))
